{
  "name": "companyhelm-runner",
  "version": "0.0.1",
  "description": "Run coding agents in fully isolated Docker sandboxes, locally.",
  "license": "Apache-2.0",
  "bin": {
    "companyhelm-runner": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "npm run build",
    "generate:codex-app-server": "codex app-server generate-ts --out src/generated/codex-app-server"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "@libsql/client": "^0.17.0",
    "commander": "^14.0.0",
    "dockerode": "^4.0.9",
    "drizzle-orm": "^0.45.1",
    "figlet": "^1.10.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/figlet": "^1.7.0",
    "@types/node": "^22.13.5",
    "typescript": "^5.8.2"
  }
}
